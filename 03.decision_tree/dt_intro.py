from sklearn import tree
from sklearn.metrics import accuracy_score
import math
x = [[0,0],[1,1]]
# y = [0,1]
# classifier = tree.DecisionTreeClassifier()
# classifier.fit(x,y)
# prediction = classifier.predict([[2.,2.]])
# print prediction
# accuracy_score(labels_test,prediction)
# classifierWith50Split = tree.DecisionTreeClassifier(min_samples_split="50")
# classifierWith2Split = tree.DecisionTreeClassifier(min_samples_split="2")

# (-(1.0/1)*(math.log((1.0/1),2)))-((0.0/2)*(math.log(0.0/2,2)))
